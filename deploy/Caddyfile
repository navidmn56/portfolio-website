{
    email {$CADDY_EMAIL}
}

{$DOMAIN} {

    encode gzip

    handle /static/* {
        root * /srv/static
        file_server
    }

    handle /media/* {
        root * /srv/media
        file_server
    }

    handle {
        reverse_proxy web:8000
    }
}